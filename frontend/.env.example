# Guardian analysis mode: real (default) or mock for deterministic offline demos
VITE_GUARDIAN_MODE=real

# Backend base URL; the adapter appends the confirmed /api/payments paths
VITE_BACKEND_URL=http://localhost:8000
